# ============================================================
# 必要環境變數（請在 Vercel Project Settings → Environment Variables 設定）
# ============================================================

# 站台共用登入密碼。建議 16+ 字元、英數字 + 符號混合。
# 改密碼：改這個值 → 已登入者繼續持有舊 cookie 到過期；想強踢所有人請同時換 AUTH_SECRET
APP_PASSWORD=請改成你想設定的密碼

# session cookie 的 HMAC 簽章 secret。**絕不可外洩**。
# 產生方式（任選）：
#   - openssl rand -hex 32
#   - node -e "console.log(crypto.randomBytes(32).toString('hex'))"
#   - https://generate-secret.vercel.app/32
# 換掉這個值 → 所有已登入 session 立即失效（緊急踢人用）
AUTH_SECRET=請改成 32+ 字元的隨機字串

# Google Gemini API key — 給 quotation OCR / 報表 OCR / Inbox AI 用
# 取得：https://aistudio.google.com/apikey （Gmail 登入，免費 1500 次/天）
GEMINI_API_KEY=

# ============================================================
# 部署到 Vercel 後的設定步驟：
# ============================================================
# 1. Vercel Dashboard → 專案 → Settings → Environment Variables
# 2. 三個都加進去（APP_PASSWORD / AUTH_SECRET / GEMINI_API_KEY）
# 3. Production / Preview / Development 三個環境都要勾
# 4. Deployments → 觸發一次 redeploy（讓新 env 生效）
# 5. 開站台 → 自動 redirect 到 /login → 用 APP_PASSWORD 登入
